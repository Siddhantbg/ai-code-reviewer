# Existing backend dependencies (add these to your current requirements.txt)
pylint==3.0.3
pylint-pydantic==0.3.2
black==23.12.1
isort==5.13.2
pre-commit==3.6.0

# Note: Add these lines to your existing backend/requirements.txt file
# Don't replace the entire file - just append these static analysis tools